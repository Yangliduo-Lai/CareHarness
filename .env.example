PORT=8766
CAREHARNESS_DATA_ROOT=./data/benchmarks
CAREHARNESS_DB_PATH=./data/careharness.sqlite
# Keys remain server-side. Provider configs refer to this variable name only.
OPENAI_API_KEY=
# Optional one-process credential used by scripts/run-matched-medmemory.js.
# The value is loaded into ModelRegistry session memory and is never persisted.
CAREHARNESS_MATCHED_API_KEY=
