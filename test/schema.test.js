import test from 'node:test';import assert from 'node:assert/strict';import{validateObservation,validateProviderConfig,SchemaError,sanitizeSecrets}from'../src/schema.js';
test('core rejects query/gold/derived as observations',()=>{for(const source_type of ['query','gold_answer','answer_options','judge_score','derived'])assert.throws(()=>validateObservation({subject_id:'p',source_type,speaker:'x',episode_id:'1',turn_id:'1',raw_text:'leak'}),SchemaError)});
test('provider rejects raw keys and sanitizes nested secrets',()=>{assert.throws(()=>validateProviderConfig({provider:'mock',model:'m',api_key:'secret'}));assert.deepEqual(sanitizeSecrets({api_key:'x',nested:{token:'y',api_key_ref:'OPENAI_API_KEY'}}),{api_key:'[REDACTED]',nested:{token:'[REDACTED]',api_key_ref:'OPENAI_API_KEY'}})});
