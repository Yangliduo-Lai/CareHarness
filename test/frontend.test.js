import test from'node:test';import assert from'node:assert/strict';import{readFileSync}from'node:fs';const html=readFileSync('public/index.html','utf8'),js=readFileSync('public/app.js','utf8'),css=readFileSync('public/styles.css','utf8');
function frontendFunctions(names){const source=names.map(name=>{const start=js.indexOf(`function ${name}(`);assert.notEqual(start,-1,`missing frontend function ${name}`);const end=js.indexOf('\nfunction ',start+1);return js.slice(start,end<0?js.length:end)}).join('\n');return Function(`${source};return {${names.join(',')}}`)()}
test('frontend removes static blue and orange explanatory notices while retaining operational status notices',()=>{for(const text of ['格式：','阶段 2 内部顺序：','connect 的合法输出包含 NO_LINK / unknown：','方法边界：','实际运行链：','只绑定具体 query：','密钥安全：','确定性组件：','可观测链：','当前边界：','独立更新：','独立 State 存储：','正式患者级实验：','官方协议：','证据索引门固定开启：','真实可见边界：','文档/实现差异：','knowledge_points：数据生成参考','summary / patient_summary / previous_admission_summary','官方 CPCD-Bench：','大实验按需加载：'])assert.equal(js.includes(text),false,text);for(const text of ['运行失败','预检通过，可以启动','当前选择：','阶段 1 Session 进度：','测试题处理中：','memory_incomplete：','连接成功','受限预览：'])assert.ok(js.includes(text),text)});
test('frontend exposes two-stage debugger, policy, Patient Graph tools and the visible benchmark pages',()=>{for(const x of ['两阶段调试器','阶段 1 · 构建历史记忆','阶段 2 · Patient–Doctor 顺序写回','Patient Graph','Action Policy','模型与 Provider','MedMemoryBench','MedLoCoMo','CPCD-Bench','Runs & Errors'])assert.ok(js.includes(x),x);assert.ok(html.includes('viewport'))});
test('frontend makes trace output, model output, errors, bundle and visible benchmark boundaries visible',()=>{for(const x of ['完整输出','模型原始输出','可操作建议','导出去密钥 Bundle'])assert.ok(js.includes(x),x);for(const x of ['Last Memory','本步完整输入','字段差异'])assert.equal(js.includes(x),false,x)});
test('frontend does not display estimated model cost',()=>{for(const x of ['已配置估算费用','cost.toFixed','cost_usd||0).toFixed'])assert.equal(js.includes(x),false,x)});
test('failed observations show their failed step instead of a missing State delta count',()=>{for(const x of ['失败步骤','未生成 State delta'])assert.ok(js.includes(x),x);assert.equal(js.includes("State deltas ${r.state_deltas??'—'}"),false)});
test('responsive layouts provide narrow-screen scrolling',()=>{assert.ok(css.includes('@media(max-width:720px)'));assert.ok(css.includes('overflow:auto'));assert.ok(css.includes('overflow-wrap:anywhere'))});
test('frontend preserves the answer channel and exposes independent scoring-only Judges',()=>{for(const x of ['API Key','仅存本次后端进程内存','/api/models/profiles','/api/models/assignments','Benchmark Answer（原 judge 通道）','MedMemory Official Judge（仅评分）','MedLoCoMo Answerable Judge（仅评分）','CPCD-Bench Official Rubric Judge（仅评分）','连接成功','编辑 / 重填 Key','删除'])assert.ok(js.includes(x),x);assert.equal(js.includes('Benchmark Answer Model'),false);assert.equal(js.includes('localStorage'),false)});

test('CPCD studio exposes official tasks and the latest-result drilldown',()=>{for(const x of ['Full counseling history → State memory → SR / MR / TCR','全部官方任务','构建完整咨询史并运行官方任务','自动读取的当前实验结果','CPCD-Bench Official Rubric Judge（仅评分）','查看完整实验结果'])assert.ok(js.includes(x),x);for(const x of ['private Client profile','online_trajectory','Client Simulator','trajectory/export'])assert.equal(js.includes(x),false,x)});
test('frontend provides run-through and real breakpoint execution with per-step output',()=>{for(const x of ['运行完整 Patient–Doctor 轮次','分段断点测试完整轮次','执行下一步','继续执行到底','本步完整输出','实际发送给模型的语义输入','完整 Prompt','模型原始输出','模型解析、重试与配置','/api/debug/conversation/start'])assert.ok(js.includes(x),x)});
test('benchmark UI keeps only meaningful controls and uses the dataset multi-hop type',()=>{assert.ok(js.includes('multi_hop_clinical_deduction'));for(const x of ['范围说明：','Session 是一次带日期的完整对话','Random seed<input','Concurrency<input','name="retry"','Cache<select','Observation 上限（可选）','<option>multi_hop</option>','name="max_session"'])assert.equal(js.includes(x),false,x)});
test('MedLoCoMo UI builds the full patient timeline, filters questions and supports frozen current-State scoring',()=>{for(const x of ['构建全部 Admission 并回答所选问题','value="single_admission"','value="cross_admission"','medical_reasoning','adversarial','完整患者时间线：','MedLoCoMo 官方指标：F1','answerable_judge_accuracy','adversarial_abstention_accuracy','combined_score',"name==='medlocomo'",'完整的全 Admission 构建','不会重新构建或修改记忆'])assert.ok(js.includes(x),x);for(const x of ['Admission range','name="max_admission"'])assert.equal(js.includes(x),false,x)});
test('MedLoCoMo automatically reads latest results and keeps literature scopes independent',()=>{for(const x of ['medLoCoMoLatestExperiment','最新实验数据 · MedLoCoMo 论文分类','Overall','Single','Cross','F1、J 只统计 answerable','Acc 只统计 adversarial','medLoCoMoLatestSources','setupMedLoCoMoLatestPanel','open-latest-medlocomo','/api/experiments/summaries?benchmark=medlocomo','medlocomo_official','answerable_f1_scored','answerable_judge_scored','adversarial_abstention_scored','combined_scored','form.elements.query_type.addEventListener'])assert.ok(js.includes(x),x);assert.ok(js.includes("scope==='overall'?mode==='all':mode==='all'||mode===scope"));assert.ok(js.includes("String(item.config?.query_type||'')===String(queryType||'')"))});
test('MedMemory page contains only the CareHarness suite, latest results, and result drilldown',()=>{for(const x of ['medmemorybench:medMemoryBenchmarkPage','function medMemoryBenchmarkPage()','id="benchResult"','id="matchedSuiteForm"','id="medLatestExperiment"',"document.querySelector('#matchedSuiteForm')",'form.elements.dev_persona.addEventListener',"item.config?.matched_experiment===true","item.config?.evaluation_mode==='static_careharness'"])assert.ok(js.includes(x),x);const start=js.indexOf('function medMemoryBenchmarkPage()'),end=js.indexOf('\nconst benchInfo=',start),page=js.slice(start,end);assert.ok(page.includes('id="benchResult"'));for(const x of ['benchForm','单样本调试 / 批量实验','preview','scoreCurrentState'])assert.equal(page.includes(x),false,x);for(const x of ['Persona → dated sessions → external query','六类题型使用官方 metric：EEM string_contain','name="persona_id"','name="start_session"','name="session_count"','构建 State 并答题','当前模式的独立 State 来源：','medStateScope'])assert.equal(js.includes(x),false,x)});
test('remaining benchmark Start renders immediate progress and actionable launch errors',()=>{for(const x of ['正在启动实验','正在校验所选数据范围','实验未启动','请检查所选 Patient / Case 与数据范围','startButton.disabled=true'])assert.ok(js.includes(x),x)});
test('MedMemory independently summarizes only Static CareHarness Clean and Noise results',()=>{for(const x of ['medLatestExperiment','Clean / Efficient (E)','With noise / Mixed (M)','item.config?.noise!==true','item.config?.noise===true','/api/experiments/summaries?benchmark=medmemorybench','MEDMEMORY_TASK_COLUMNS','EEM','TLA','SUA','MQ','IG','MCD','Avg.','六类 query 动态明细','官方 metric','待评 / 不可评','setupMedMemoryLatestPanel','open-latest-medmemory','Number.isFinite(item.score)',"item.config?.matched_experiment===true","item.config?.evaluation_mode==='static_careharness'"])assert.ok(js.includes(x),x);for(const x of ['最新实验数据 · 论文表格视图','独立保存和刷新','Number.isFinite(Number(item.score))','experiments=preferred?[preferred]'])assert.equal(js.includes(x),false,x);for(const x of ['latest-experiment-head','literature-result-table','score-average'])assert.ok(html.includes(x),x)});
test('MedMemory frontend configures and observes the Static CareHarness suite in the same server process',()=>{for(const x of ['CareHarness-first · Static CareHarness 运行控制台','仅预检（不调用模型）','启动 1 个 CareHarness 运行','同进程模型与 Key','static_careharness','Candidate budget','Action budget','Failure taxonomy 聚合','打开 Experiment：逐题看 action / prompt / scorer','/api/matched-suites/preflight','/api/matched-suites','medMemoryMatchedTimer','v13_ready','x.result?.careharness_rows',"step.phase==='careharness_run'","item.version==='medmemory-matched-suite.v4'"])assert.ok(js.includes(x),x);for(const x of ['当前运行固定为 97 题 Static CareHarness','不声称持久化 cross-state graph','baseline_rows',"step.phase==='baseline'"])assert.equal(js.includes(x),false,x);for(const x of ['matched-options','matched-steps','matched-live'])assert.ok(css.includes(x),x)});
test('matched frontend fixes Static CareHarness and computes only Dev Persona Clean/Noise scopes',()=>{for(const x of ['name="matched_condition" type="checkbox" value="clean" checked','name="matched_condition" type="checkbox" value="noise">Noise','function matchedSuiteSelection(form)','scopeCount=config.conditions.length','runCount=scopeCount','queryCount=scopeCount*97','个数据 scope × 97 题','matchedSelectionCost','selection.confirm','CareHarness 运行结果'])assert.ok(js.includes(x),x);for(const x of ['const MATCHED_MODES=','name="matched_mode"','checked(\'matched_mode\')','modeLabels','config.modes','modes:[','<th>Mode</th>','row.mode','Direct · question-only','Long context · all visible raw','BM25 · raw top-k','Same-model baselines','每个所选 mode 的 97 题'])assert.equal(js.includes(x),false,x);const{matchedSuiteConfig,matchedSuiteSelection}=frontendFunctions(['matchedSuiteConfig','matchedSuiteSelection']);let conditions=['clean'];const form={querySelectorAll:selector=>{assert.equal(selector,'input[name="matched_condition"]:checked');return conditions.map(value=>({value}))},elements:{dev_persona:{value:'1'},prepare_snapshots:{checked:true},seed:{value:'42'},candidate_budget:{value:'24'},action_budget:{value:'6'}}},config=matchedSuiteConfig(form);assert.deepEqual(Object.keys(config).sort(),['action_budget','candidate_budget','conditions','dev_persona','prepare_snapshots','seed']);assert.deepEqual({scopes:matchedSuiteSelection(form).scopeCount,runs:matchedSuiteSelection(form).runCount,queries:matchedSuiteSelection(form).queryCount},{scopes:1,runs:1,queries:97});conditions=['clean','noise'];assert.deepEqual({scopes:matchedSuiteSelection(form).scopeCount,runs:matchedSuiteSelection(form).runCount,queries:matchedSuiteSelection(form).queryCount},{scopes:2,runs:2,queries:194})});
test('experiment result layer explains an empty query result',()=>{for(const x of ['没有测试结果','query_message'])assert.ok(js.includes(x),x);for(const x of ['没有匹配测试题','首个可用测试题位于 Session'])assert.equal(js.includes(x),false,x)});
test('benchmark UI reports scoring progress and makes query-bound Working Subgraph details inspectable',()=>{for(const x of ['正在回答测试题','session_scoring','每个 Session 构建完成后立即回答该 Session 的题目','继续构建下一 Session','formatDuration','小时','分钟','候选直通检索汇总指标','Query Planner 模型输出','Candidate 直通阶段、通道与分数','完整 Retrieval Trace','完整 Runtime Context','Query-bound Working Subgraph','workingRuntimeView(runtime','retrieval_trace','retrieval_context'])assert.ok(js.includes(x),x);assert.equal(js.includes('State Selector / Rerank 阶段'),false)});
test('Evidence Index Gate remains mandatory and the removed decision layer has no frontend controls or trace panel',()=>{for(const x of ['证据索引门 · 固定索引、证据链与覆盖补检'])assert.ok(js.includes(x),x);for(const x of ['name="evidence_index_gate"','x.evidence_index_gate=Boolean','证据索引门 ${x.config.evidence_index_gate?','name="clinical_safety_gate"','name="understanding_clarification_gate"','name="preference_feasibility_gate"','临床需求与安全门','理解与澄清门','偏好与可行性门','三道可选决策门 · 输出已参与答案生成','G1 · clinical safety','G2 · clarification','G3 · preference feasibility'])assert.equal(js.includes(x),false,x);assert.ok(html.includes('.checkbox-field'))});
test('frontend provides phase 1 Session-level breakpoint progress',()=>{for(const x of ['阶段 1 分段断点测试','/api/debug/memory/start','阶段 1 Session 进度','停在完整 Session 边界','阶段 1 各 Session Run'])assert.ok(js.includes(x),x)});
test('frontend exposes lifecycle, Patient Graph reset and keeps conversation and benchmark policies separate',()=>{for(const x of ['/api/memory/build','/api/conversation/run','/api/memory/','清空该患者 State 记忆','从空记忆开始','Conversation response policy','Benchmark CareHarness runtime policy'])assert.ok(js.includes(x),x);assert.ok(js.includes('这个交互入口不会声称构造 benchmark W<sub>q</sub>'));assert.equal(js.includes('workingRuntimeView(final)'),false)});

test('experiment history loads a compact view and fetches each complete answer trace on demand',()=>{for(const x of ['查看完整实验输出','题目与评分输出','系统实际输出','Gold 标准答案','评分结果与方法','Answer Model 完整 Prompt、原始输出与解析结果','官方 Judge 独占输入（答案冻结后才可见）','独立 Judge Model 完整 Prompt、原始输出与解析结果','记忆构建 observation','查看每一步输入输出','当前页面结果索引（完整 trace 按需读取）','/view','/scores/','只加载当前题目的完整 trace','experimentDrilldown'])assert.ok(js.includes(x),x);assert.equal(js.includes('完整原始实验结果 JSON'),false);assert.equal(js.includes("api('/api/experiments')"),false)});
test('experiment UI exports wrong answers, every non-perfect CPCD score, and incomplete memory',()=>{for(const x of ['wrongAnswerCount','一键导出错题与完整诊断','暂无错题可导出','wrong-answers/export','道错题/不可评分题','memory_incomplete','记忆构建完整性诊断','受影响题目不会调用答题模型',"item.scoring_method==='cpcd_bench_official_llm_judge'",'item.score<1',"x.benchmark==='cpcdbench'&&t.scores>0",'导出未满分题与诊断'])assert.ok(js.includes(x),x)});
test('experiment UI counts failed queries and honestly labels retries with their data/model semantics',()=>{for(const x of ["item.status==='failed'",'x.progress.query_failed',"x.status==='failed'",'x.progress.fatal_error','按原数据范围、使用当前模型完整重跑','已按原数据范围、使用当前模型创建完整重跑实验'])assert.ok(js.includes(x),x);for(const x of ['>失败样本重跑</button>','按原配置完整重跑','已按原配置创建完整重跑实验'])assert.equal(js.includes(x),false,x)});
test('experiment history distinguishes parallel preprocessing from ordered commits',()=>{for(const x of ['预处理','preprocess_completed','preprocess_total','提交 ${x.progress.completed}'])assert.ok(js.includes(x),x)});
test('Patient Graph Explorer renders one graph with six typed node families and evidence-bound edges',()=>{for(const x of ['Patient Graph Explorer',"['BC','PE','PA','CS','CP','LO']",'familyName(family)','patient-graph-canvas','graph-lanes','Node · version · provenance','Edge · confidence · provenance','temporal','clinical_care','Evidence + confidence 必填','contributes_to ≠ 严格因果'])assert.ok(js.includes(x)||css.includes(x),x);assert.equal(js.includes('风险语义三栏'),false)});
test('Patient Graph Explorer switches views and removes graph nodes and edges through memory APIs',()=>{for(const x of ["api('/api/states')",'stateSubject','states(event.target.value,view)','data-graph-view','Current valid graph','完整版本图','Episode timeline','删除该患者 Patient Graph','Patient Graph nodes / edges','历史 Run 和 trace 会保留','/api/memory/'])assert.ok(js.includes(x),x)});
test('frontend separates three controls from selective graph actions and permits NO_LINK',()=>{for(const x of ['C_scope','C_time','C_rel',"const GRAPH_ACTIONS=['focus','trace','connect','evaluate','verify','answer']",'且不是固定六步','NO_LINK / unknown','action-flow-runtime','action-trace-list','W<sub>q</sub> = Φ(q, G<sub>t</sub>)'])assert.ok(js.includes(x)||css.includes(x),x);assert.equal(js.includes("const GRAPH_ACTIONS=['focus','trace','connect','prove','verify','answer']"),false)});
test('Current Patient Graph view forms complete lineage components before evaluating their latest status',()=>{for(const x of ['function currentGraphHeads(nodes,edges)','const byId=new Map','const parent=new Map','node.predecessor_state_id','node.successor_state_id','node.version_chain','node.supersedes','node.conflicts_with','node.resolves',"new Set(['updates','persists','supersedes','resolves','conflicts','recurs'])",'const factorRepresentative=new Map()','const latest=newestGraphNodes(items)[0]',"invalid.has(String(latest.status||'').toLowerCase())",'function newestGraphNodes(nodes)','compareGraphTime(b,a)','previewNodes=nodes.slice(0,300)'])assert.ok(js.includes(x),x);assert.ok(js.indexOf('const groups=new Map(),orphans=[]')<js.indexOf('invalid.has(String(latest.status'));assert.ok(js.indexOf('currentGraphHeads(allNodes,allEdges)')<js.indexOf('previewNodes=nodes.slice(0,300)'))});
test('Current Patient Graph behavior never falls back behind a legacy conflict or resolution',()=>{const{currentGraphHeads}=frontendFunctions(['graphNodeId','graphEdgeEndpoint','graphSessionNumber','compareGraphTime','newestGraphNodes','currentGraphHeads']),node=(id,session,status='active',extra={})=>({state_id:id,subject_id:'legacy-patient',family:'CS',status,event_time:`2025-0${session}-01`,episode_id:`session-${session}`,version:session,evidence_ids:[`e-${id}`],version_chain:[],...extra});for(const status of ['conflict','resolved']){const oldest=node('oldest',1),latest=node('latest',3,status,{version_chain:['oldest'],conflicts_with:status==='conflict'?'oldest':null,resolves:status==='resolved'?'oldest':null}),backfill=node('backfill',2,'active',{version:4,version_chain:['oldest']});assert.deepEqual(currentGraphHeads([oldest,latest,backfill],[]),[],status)}const latest=node('latest-active',3),backfill=node('older-backfill',1,'active',{version:4,successor_state_id:'latest-active'});assert.deepEqual(currentGraphHeads([latest,backfill],[]).map(item=>item.state_id),['latest-active'])});
test('Patient Graph Explorer never fabricates a latest runtime and points to a concrete query detail',()=>{for(const x of ['Query-bound Working Subgraph','Persistent G<sub>t</sub> 与 runtime W<sub>q</sub> 不混存','都绑定某一道具体 query','去 MedMemoryBench 打开具体题目'])assert.ok(js.includes(x),x);assert.equal(js.includes('runtime=data.latest_runtime||data.runtime_context'),false)});
test('runtime presentation only renders a query-bound Static CareHarness Working State',()=>{for(const x of ["value?.evaluation_mode!=='static_careharness'||!value?.working_state",'W<sub>q</sub> = Φ(q, G<sub>t</sub>)','C_scope','C_time','C_rel','action-flow-runtime'])assert.ok(js.includes(x),x);for(const x of ["value?.evaluation_mode==='general_harness'",'function genericActionTraceView(value)','General harness trace','no Patient Graph Wq / CareHarness controls'])assert.equal(js.includes(x),false,x);assert.equal(css.includes('generic-runtime'),false)});
test('Static CareHarness query detail exposes the semantic relation evaluator contract',()=>{for(const x of ['semanticRelationEvaluatorView(trace,runtime)','Query-time semantic relation evaluator','skipped_verification_blocked','model_calls','accepted','rejected','NO_LINK / unknown','未形成 NO_LINK 判定','Semantic relation evaluator · model trace','Semantic evaluator artifact · accepted / rejected','evaluator 没有成功完成，不能把跳过或失败伪装成 NO_LINK','Error：'])assert.ok(js.includes(x),x);const{semanticRelationEvaluatorArtifact}=frontendFunctions(['semanticRelationEvaluatorArtifact']),runtime={evaluation_mode:'static_careharness',working_state:{evidence:{semantic_relation_evaluation:{accepted_relations:[{edge_id:'q1'}],rejected_relations:[{reason:'bad quote'}],no_link_reason:''}}},relations:[{source:'query_time_relation_evaluator'}]},completed=semanticRelationEvaluatorArtifact({semantic_relation_evaluator:{status:'completed',model_calls:1,accepted_relation_count:1,rejected_relation_count:1,no_link:false,model_trace:{provider:'test'}}},runtime);assert.deepEqual({status:completed.status,calls:completed.model_calls,accepted:completed.accepted,rejected:completed.rejected,no_link:completed.no_link,trace:completed.model_trace.provider},{status:'completed',calls:1,accepted:1,rejected:1,no_link:false,trace:'test'});const blocked=semanticRelationEvaluatorArtifact({semantic_relation_evaluator:{status:'skipped_verification_blocked',model_calls:0,blocking_rejected_states:2,blocking_rejected_relations:1}},runtime);assert.equal(blocked.status,'skipped_verification_blocked');assert.match(blocked.reason,/2 states, 1 relations/);assert.equal(blocked.no_link,false);const failed=semanticRelationEvaluatorArtifact({semantic_relation_evaluator:{status:'failed',model_calls:1,error:'provider unavailable'}},runtime);assert.deepEqual({status:failed.status,calls:failed.model_calls,error:failed.error,no_link:failed.no_link},{status:'failed',calls:1,error:'provider unavailable',no_link:false});assert.equal(semanticRelationEvaluatorArtifact({},runtime).status,'not_run')});
test('graph preview communicates candidate and causal boundaries without rendering the full payload',()=>{for(const x of ['causality not asserted','candidate，不可直接进入 reasoning','受限 Patient Graph preview JSON',"preview_policy:'latest_300_nodes_then_endpoint_bound_300_edges'",'graphTimeline(previewNodes)','.graph-edge.candidate{border-style:dashed'])assert.ok(js.includes(x)||css.includes(x),x);assert.equal(js.includes("details('完整 Patient Graph JSON',graph)"),false);assert.equal(js.includes(' · non-causal'),false)});
test('graph chronology falls back to numeric Session ordering',()=>{for(const x of ['function graphSessionNumber(node)',"/(?:session|episode|admission|encounter)[-_ ]?(\\d+)/i",'if(as!=null&&bs!=null&&as!==bs)return as-bs'])assert.ok(js.includes(x),x)});
