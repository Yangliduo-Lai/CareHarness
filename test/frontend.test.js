import test from'node:test';import assert from'node:assert/strict';import{readFileSync}from'node:fs';const html=readFileSync('public/index.html','utf8'),js=readFileSync('public/app.js','utf8'),css=readFileSync('public/styles.css','utf8');
test('frontend exposes all required primary tools and four distinct benchmark pages',()=>{for(const x of ['Pipeline 调试器','State Explorer','Three-Gate Inspector','模型与 Provider','MedMemoryBench','MedLoCoMo','MusPsy','PsychEval','Runs & Errors'])assert.ok(js.includes(x),x);assert.ok(html.includes('viewport'))});
test('frontend makes raw trace, model output, errors, bundle and visibility boundaries visible',()=>{for(const x of ['完整输入','完整输出','模型原始输出','可操作建议','导出去密钥 Bundle','knowledge_points','Last Memory','client_info_last'])assert.ok(js.includes(x),x)});
test('responsive layouts provide narrow-screen scrolling',()=>{assert.ok(css.includes('@media(max-width:720px)'));assert.ok(css.includes('overflow:auto'));assert.ok(css.includes('overflow-wrap:anywhere'))});
